syntax = "proto3";

message Accout {
    uint32 id = 1;
    string name = 2;
    bytes thumbnail = 3;
    bool is_verified = 4;
    float height = 5;

    // repeated fields represent lists
    // var phones []string
    repeated string phones = 6;
}